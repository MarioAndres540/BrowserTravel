@model IEnumerable<BrowserTravel.Entidades.Editorials>

@{
    ViewData["Title"] = "Index";
}

<h1>Editorials</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-controller="Editorials" asp-action="Index">
    <p>
        Search: <input type="text" name="buscar" class="form-text" />
        <input type="submit" value="Search" class="btn btn-sm btn-dark" />
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th @Html.DisplayNameFor(model => model.Nombre)>
                Name
            </th>
            <th @Html.DisplayNameFor(model => model.Sede)>
                Publishing house
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sede)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
